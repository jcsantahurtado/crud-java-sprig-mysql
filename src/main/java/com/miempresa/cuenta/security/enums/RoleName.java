package com.miempresa.cuenta.security.enums;

public enum RoleName {
    // El aplicativo tiene dos roles:
    // admin(Tiene todos los permisos CRUD) y user(ve las transferencias/movimientos)
    ROLE_ADMIN, ROLE_USER
}
